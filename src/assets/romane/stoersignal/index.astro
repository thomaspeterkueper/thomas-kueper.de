---
// Dateipfad: src/pages/romane/stoersignal/index.astro
// Name: Störsignal - Detailseite
// Beschreibung: Psychothriller über Resonanz und Kontrolle.

// 1. Bild importieren (Bild muss im selben Ordner liegen!)
import coverImage from './cover.jpg';

// 2. WICHTIG: Export für die Archivseite (Astro.glob)
export const frontmatter = {
  title: "Störsignal",
  genre: "Psychothriller",
  description: "Martina Fischer erhält ein Paar schlichte Ohrringe, die eine Frequenz senden, die sich wie die Abwesenheit von Einsamkeit anfühlt. Ein Roman über die dunkle Seite der Resonanz und die schleichende Kontrolle über das eigene Ich.",
  status: "Fokus / Neuerscheinung",
  order: 1, // Erscheint als erstes im Archiv
  cover: coverImage.src // Reicht den Bildpfad an die Übersicht weiter
};

// Layout importieren (3 Ebenen zurück: index -> stoersignal -> romane -> pages)
import BaseLayout from '../../../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Störsignal — Psychothriller von Thomas Peter Küper" 
  description={frontmatter.description}>

  <style is:global>
    body, html, main { background-color: #ffffff !important; }
  </style>

  <style>
    .ss-page { color: #1a1a1a; font-family: 'Noto Sans', sans-serif; }
    
    .ss-hero {
      max-width: 1200px;
      margin: 0 auto;
      padding: 6rem 2rem;
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 5rem;
      align-items: start;
    }

    .ss-cover-wrapper {
      position: sticky;
      top: 120px;
    }

    .ss-cover-img {
      width: 100%;
      height: auto;
      border-radius: 2px;
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
    }

    .ss-hero-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2.5rem, 5vw, 4.5rem);
      text-transform: uppercase;
      letter-spacing: 0.12em;
      margin: 0;
      font-weight: 300;
    }

    .ss-hero-genre {
      font-size: 0.9rem;
      letter-spacing: 0.35em;
      text-transform: uppercase;
      color: #c4a862;
      font-weight: 600;
      margin: 1rem 0 3.5rem;
    }

    .ss-description {
      font-family: 'Libre Baskerville', serif;
      font-size: 1.15rem;
      line-height: 2.2;
    }

    .ss-dark-section {
      background: #0a0a0a;
      color: #fff;
      padding: 8rem 2rem;
      text-align: center;
      margin-top: 6rem;
    }

    .ss-question {
      font-family: 'Cormorant Garamond', serif;
      font-style: italic;
      font-size: 1.8rem;
      color: #c4a862;
      max-width: 800px;
      margin: 0 auto 3rem;
    }

    .btn-back {
      display: inline-block;
      margin-top: 2rem;
      color: #c4a862;
      text-decoration: none;
      border: 1px solid #c4a862;
      padding: 0.8rem 2rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      font-size: 0.8rem;
    }

    @media (max-width: 850px) {
      .ss-hero { grid-template-columns: 1fr; text-align: center; }
      .ss-cover-wrapper { position: relative; top: 0; margin: 0 auto 3rem; max-width: 250px; }
    }
  </style>

  <div class="ss-page">
    <section class="ss-hero">
      <div class="ss-cover-wrapper">
        <!-- Das Cover auf der Detailseite -->
        <img src={coverImage.src} alt="Cover Störsignal" class="ss-cover-img" />
      </div>

      <div class="ss-hero-text">
        <h1 class="ss-hero-title">{frontmatter.title}</h1>
        <p class="ss-hero-genre">{frontmatter.genre}</p>
        
        <div class="ss-description">
          <p>Martina Fischer ist achtzehn, als ihr Onkel Erik ihr in Amsterdam ein Paar Ohrringe schenkt. Sie sind schlicht, fast unscheinbar. Was sie auslösen, ist es nicht.</p>
          <p>Eine Frequenz, ein Gefühl, eine Abhängigkeit, die sich anfühlt wie die Abwesenheit von Einsamkeit.</p>
          <p><em>Störsignal</em> erzählt von der Grenze zwischen Fürsorge und Übergriff, zwischen Resonanz und Kontrolle.</p>
        </div>
      </div>
    </section>

    <section class="ss-dark-section">
      <div class="ss-question">
        »Wenn jemand deine Frequenz überschreibt, wer bist du dann noch?«
      </div>
      <a href="/romane/standalone" class="btn-back">← Zurück zum Archiv</a>
    </section>
  </div>
</BaseLayout>